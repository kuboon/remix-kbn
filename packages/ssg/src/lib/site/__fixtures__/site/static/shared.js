// The module both entry chunks import. A crawler that read only markup would never reach it —
// nothing links to this file, and only an `import` names it.
export const label = 'fixture-shared-module'
